test: integrate map container and upgrade dual-type engine

- Refactors cgen_app_run_dual to support position-independent directory routing
- Implements map_{{KEY_B}}_{{VAL_B}}_deep_free lifecycle utility in templates
- Fixes internal struct and comment syntax typos inside cgen-map templates
- Integrates isolated test_map validations into automated integration suite
